# OBS Plugins snaps - Meta

## Repo automation

This repo contains stateless GitOps repo reconciliation
using OpenTofu (`import`-on-demand) and `just`.

For now, it helps keeping existing repos in sync.
